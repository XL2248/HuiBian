DATA SEGMENT
	X DW ?
	Y DW ?
DATA ENDS
CODE SEGMENT
	ASSUME CS:CODE,DS:DATA
START:MOV AX,DATA
	MOV DS,AX
	MOV X,-2
	CMP X,0
	JGE BIN
	MOV Y,-1
	JMP NEXT
BIN:JZ EQUL
	MOV Y,1
	JMP NEXT
EQUL:MOV Y,0
NEXT:MOV AH,4CH
INT 21H
CODE ENDS
	END START
DATA SEGMENT
	X DW 4H
	Y DW 7H
	Z DW 3H
	S DW 2 DUP(?) ;
DATA ENDS
CODE SEGMENT
	ASSUME DS:DATA,CS:CODE 
START:    MOV AX,DATA
	  MOV DS,AX 
	  MOV DX,0
	  MOV AX,X
	  ADD AX,Y
	  ADC DX,0 
	  ADD AX,Z
	  ADC DX,0
	  MOV S+2,DX 
	  MOV S,AX
	  MOV AX,4C00H
	  INT 21H
CODE      ENDS
	  END START
DATA SEGMENT ;统计空格个数
	ATR1 DB,'HFSDKL SFDKLH FDKLJ'
DATA ENDS
CODE SEGMENT
	ASSUME CS:CODE,DS:DATA
	START:MOV AX,DATA
	MOV DS,AX
	MOV CX,13H
	XOR DI,DI
	XOR SI,SI
COMP:CMP ATR1[SI],20H
	INC SI
	JNE NEXT
	INC DI
NEXT:LOOP COMP
	MOV DL,DI
	MOV AH,2
	INT 21H
	MOV AH,4CH
	INT 21H 
CODE ENDS
	END START